// Global type definitions for window.api stub
// This allows the frontend to build while we migrate away from Electron

declare global {
    interface Window {
        api: any;
    }
}

export { };
