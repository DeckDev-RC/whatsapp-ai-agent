# Server Configuration
NODE_ENV=production
PORT=3000

# CORS (opcional - se quiser restringir origem)
# CORS_ORIGIN=https://savycore.com.br

# Nota: API keys e configurações do Supabase serão gerenciadas via interface web
# e armazenadas no banco de dados (criptografadas)
